/**
 * API Hooks - Re-export from root
 * 
 * These hooks wrap API service calls.
 * Located at root level for now, will be migrated later.
 */

export { useAuth } from '../../features/authentication/context/AuthContext';
